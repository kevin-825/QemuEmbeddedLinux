EXAMPLE_APP_VERSION = local
EXAMPLE_APP_SITE = $(BR2_EXTERNAL_QEMUEMBEDDEDLINUX_PATH)/package/apps/example_app/src
EXAMPLE_APP_SITE_METHOD = local

define EXAMPLE_APP_BUILD_CMDS
	$(MAKE) $(TARGET_CONFIGURE_OPTS) -C $(\@D) all
endef

define EXAMPLE_APP_INSTALL_TARGET_CMDS
	$(MAKE) $(TARGET_CONFIGURE_OPTS) -C $(\@D) DESTDIR=$(TARGET_DIR) install
endef

$(eval $(generic-package))
