EXAMPLE_DRIVER_VERSION = local
EXAMPLE_DRIVER_SITE = $(BR2_EXTERNAL_QEMUEMBEDDEDLINUX_PATH)/package/drivers/example_driver/src
EXAMPLE_DRIVER_SITE_METHOD = local

define EXAMPLE_DRIVER_BUILD_CMDS
	$(MAKE) $(TARGET_CONFIGURE_OPTS) -C $(\@D) all
endef

define EXAMPLE_DRIVER_INSTALL_TARGET_CMDS
	$(MAKE) $(TARGET_CONFIGURE_OPTS) -C $(\@D) DESTDIR=$(TARGET_DIR) install
endef

$(eval $(generic-package))
